version: '3.8'

services:
  mass-flasher:
    build:
      context: ..
      dockerfile: mass_flasher/Dockerfile
    container_name: mass_flasher_app
    privileged: true
    user: root
    ports:
      - "5000:5000"
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ../controlboard:/app/controlboard_repo
    environment:
      - PYTHONUNBUFFERED=1
